<p id="notice"><%= notice %></p>
<table>
	<thead>
		<th>/</th>
	</thead>
	<tbody>
		<% @salary.each do |salary| %>
		<tr>
		<% salary.salary_details.each do |key, value| %>
		<td>
			<b><%= "#{key}" %></b>
		</td>
		<td>
			<b><%= "#{value}" %></b>
		</td>
		<% end %>
		</tr>
		<% end %>
	</tbody>
</table>