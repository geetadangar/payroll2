<p id="notice"><%= notice %></p>

<h1>import</h1>
<table>
  <thead>
    <tr>
      <th>select company</th>
      <%# raise @company.inspect %>
      <th><%= select_tag "company_id", options_from_collection_for_select(@company, "id", "name") %></th>
      <th colspan="3"></th>
    </tr>
  </thead>
  
  <tbody>
    <% @company.each do |company| %>
      <tr>
        <td><%= company.name %></td>
      </tr>
    <% end %>
  </tbody>
</table>
<%= link_to 'New ', new_import_path %>

